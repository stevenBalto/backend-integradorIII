# Antierrores — Backend

Catálogo de errores del backend. Cada vez que se corrige un error, se documenta aquí para que NO se repita en la próxima sesión.

Cómo se llena: una entrada por error corregido, con la regla a no romper.

Formato sugerido por entrada:
```
### EB-01 — <título corto>
- Qué pasó: <descripción del error>
- Causa: <por qué pasó>
- Regla: <qué hacer siempre / nunca para no repetirlo>
- Fecha: YYYY-MM-DD
```

### EB-01 — personal_access_tokens debe ser polimórfica (Sanctum)
- Qué pasó: el esquema reconstruido tenía `personal_access_tokens.user_id` (FK a `users`), pero Laravel Sanctum requiere columnas polimórficas `tokenable_type` + `tokenable_id`. Con `user_id`, `createToken()` falla.
- Causa: la tabla del ERD no siguió el formato estándar de Sanctum (polimórfico).
- Regla: para auth con Sanctum, `personal_access_tokens` SIEMPRE va polimórfica (`tokenable_type`/`tokenable_id`, sin FK a `users`). No modelarla con `user_id`.
- Fecha: 2026-06-28 (migración en `bd-doc/migracion_2026-06-28_sanctum_personal_access_tokens.sql`)

### EB-02 — Instancia de BD "fresca" sin el fix EB-01 aplicado ni roles sembrados
- Qué pasó: al retomar el proyecto en otra máquina/instancia de Postgres, `.env` apuntaba a una BD inexistente (`rooster_pizza_grill` en vez de `rooster_pizza`) y sin `DB_PASSWORD`. Al corregir eso, aparecieron dos problemas más en cascada: (1) `roles` y `users` estaban completamente vacíos (ni siquiera el login normal funcionaba), y (2) `personal_access_tokens` de ESA instancia todavía tenía el esquema viejo con `user_id` — el fix de EB-01 se había aplicado en otra BD, no en esta. Un CRUD nuevo (productos) parecía "no persistir" cuando en realidad ninguna request autenticada llegaba a completarse.
- Causa: la BD se carga por SQL manual (`rooster_pizza_bd.sql`), no por migraciones versionadas — cada instancia nueva de la BD requiere reaplicar a mano tanto el seed de roles como cualquier parche posterior (como el de EB-01). No hay una única fuente de verdad ejecutable que garantice que todas las instancias queden alineadas.
- Regla: al levantar el proyecto en una BD nueva (o sospechar que el entorno está desalineado), verificar en este orden ANTES de asumir que hay un bug de código: (1) `DB_DATABASE`/`DB_PASSWORD` correctos en `.env` (confirmar con `\dt` o consulta a `information_schema.tables`); (2) `roles` y al menos un usuario admin sembrados (`php artisan db:seed --class=RolesSeeder` + `AdminTestUserSeeder`); (3) columnas de `personal_access_tokens` son `tokenable_id`/`tokenable_type` (si no, reaplicar el forward de `migracion_2026-06-28_sanctum_personal_access_tokens.sql` a mano vía `DB::statement`). Si el login funciona pero cualquier request protegido devuelve 401/500 encriptado, sospechar de este checklist antes de tocar controllers/services.
- Fecha: 2026-07-10

### EB-03 — POST /api/register 500 tras la migración multi-tenant (instancia_id NOT NULL)
- Qué pasó: al probar el flujo de checkout de Pedidos, cualquier registro de cliente nuevo tiraba 500 (`Not null violation: instancia_id`). El registro llevaba semanas roto sin que nadie lo hubiera detectado porque los clientes de prueba ya existían de antes.
- Causa: `UserRepository::crearCliente()` nunca fue actualizado cuando el compañero migró `users.instancia_id` a NOT NULL (fase F0 del multi-tenant). El `User::create([...])` seguía sin esa columna.
- Regla: cualquier columna NOT NULL nueva en una tabla con `create()`/inserts existentes en el código hay que rastrearla contra TODOS los sitios que insertan en esa tabla, no solo los que se estén tocando en la tarea actual. `crearCliente()` ahora asigna `instancia_id = 1` (constante `UserRepository::INSTANCIA_DEFAULT`) porque hoy solo existe un negocio real; si en el futuro el registro público necesita elegir instancia, hay que rediseñar esto (no hay forma de que un cliente anónimo sepa a qué tenant pertenece sin un subdominio/selector).
- Fecha: 2026-07-16

### EB-04 — CACHE_STORE=database sin tabla `cache` rompía TODO throttle (403/500 silencioso)
- Qué pasó: al agregar `throttle:10,1` al nuevo endpoint público `GET /pedidos/buscar`, cualquier request tiraba 500 (`Undefined table: cache`). Investigando, resultó que `/forgot-password`/`/reset-password` (ya existentes, con `throttle:6,1` desde antes) tenían EXACTAMENTE el mismo problema — no era un bug nuevo, ya estaba roto.
- Causa: `.env` tenía `CACHE_STORE=database` pero nunca se creó la tabla `cache` (Laravel usa el cache para contar intentos del rate limiter). Sin esa tabla, cualquier ruta con `throttle:` explota en vez de simplemente no limitar.
- Regla: igual que `SESSION_DRIVER` (ver nota en `HiloActualBack.md` sesión 2026-06-29), este proyecto NO tiene tablas de `cache`/`jobs` — mantener `CACHE_STORE=file` y `QUEUE_CONNECTION=sync` en `.env` (ya está así en `.env.example`, pero el `.env` local se había desviado). Antes de agregar `throttle:` a una ruta nueva, probar esa ruta primero — si tira 500 con "Undefined table: cache", es este problema, no el código nuevo.
- Fecha: 2026-07-16

### EB-05 — Sucursales sin CRUD: cualquier instancia nueva queda con selector de sucursal vacío
- Qué pasó: el usuario reportó que el selector de sucursal del carrito "no muestra sucursales acorde a la instancia". Al investigar, `sucursales` solo tenía 1 fila (sembrada a mano para la instancia 1 en la migración de Pedidos) y `SucursalController` solo tenía `index()` (lectura). Cualquier instancia nueva (había una segunda, "CHRISTIAN", creada por el compañero vía superadmin) queda con CERO sucursales sin forma de crear una — el selector del carrito para esos usuarios está vacío por diseño, no por un bug de filtrado (el filtrado por `instancia_id` vía `PerteneceAInstancia` siempre funcionó correctamente).
- Causa: al construir el módulo Pedidos (sesión 2026-07-16) se sembró 1 sucursal solo para destrabar el flujo de la instancia 1, y el CRUD completo de Sucursales quedó explícitamente fuera de alcance ("módulo futuro", ver `HiloActualBack.md`). Nadie conectó ese pendiente con el hecho de que el multi-tenant del compañero ya permitía crear instancias nuevas sin sucursal propia.
- Regla: cuando se agrega la capacidad de crear tenants/instancias nuevas (multi-tenant), revisar qué tablas "de arranque" (sucursales, categorías, etc.) dependen de un seed manual y no tienen su propio flujo de alta — un tenant nuevo debe poder autoabastecerse de esos datos básicos sin intervención manual en la BD. Se agregó `POST`/`PUT /admin/sucursales[/{id}]` (CRUD mínimo, ver sesión 2026-07-17) para cerrar este hueco.
- Fecha: 2026-07-17

### Hallazgo (no corregido en esta sesión) — GET /api/productos público mezcla instancias sin sesión
- Qué pasó: al revisar el catálogo público para el carrito, `GET /api/productos` sin token de auth devuelve productos de TODAS las instancias mezclados, porque `PerteneceAInstancia` solo filtra si hay un usuario autenticado (`Auth::user()`) — un visitante anónimo no tiene `instancia_id`, así que el scope simplemente no aplica.
- Causa: decisión de arquitectura del multi-tenant del compañero — el catálogo público nunca se diseñó pensando en múltiples instancias con catálogos propios navegables sin login.
- Por qué no se arregla aquí: hoy es inofensivo (solo la instancia 1 tiene productos reales), y arreglarlo implica decidir un modelo de acceso público multi-tenant (¿subdominio por negocio? ¿selector de negocio en el home?) que le corresponde a quien diseñó el multi-tenant, no a esta tarea de Pedidos.
- Fecha: 2026-07-16

### EB-06 — Gotchas de Eloquent en seeders (fillable, trait, autoasignacion de instancia_id)
- Qué pasó: al crear `ClientesDemoSeeder` (para poblar BD con datos de prueba del modulo Clientes), aparecieron 3 bloqueantes inesperados que NO se habian encontrado en seeders previos (`RolesSeeder`, `AdminTestUserSeeder`): (1) `Pedido::create()` tiraba error de mass-assignment porque `$fillable = []` (en la rama donde se escribió este seeder, `Pedido` era de solo lectura para el módulo Clientes — al mergear con el módulo Pedidos, `Pedido::$fillable` ya tiene las columnas reales, ver `HiloActualBack.md`), (2) `User::create()` no persistia `puntos_balance` aunque el campo existe en BD, (3) `Sucursal::create()` tiraba error de constraint NOT NULL en `instancia_id` porque, EN ESE MOMENTO, `Sucursal` no usaba el trait `PerteneceAInstancia` (eso se agregó en paralelo en la sesión de Pedidos del 2026-07-16 — tras el merge, `Sucursal` YA autoasigna `instancia_id`, este workaround puntual puede no ser necesario si se vuelve a correr el seeder desde cero).
- Causa:
  - `User::$fillable` NO incluye `puntos_balance` — hay que asignarlo por propiedad + `save()` despues del `create()`.
  - El trait `PerteneceAInstancia` (que usan `User`/`Pedido`/`Producto`/`Insumo`/`Oferta`/`Sucursal`/`Extra`) autoasigna `instancia_id` leyendo `Auth::user()` en el hook `creating()` — en un seeder/comando de consola no hay usuario autenticado, asi que esto NO se autoasigna solo. Cualquier seeder que cree filas de estos modelos tiene que setear `instancia_id` manualmente.
- Regla:
  - Antes de escribir un seeder/comando que cree filas de un modelo, verificar: (1) si `$fillable` esta vacio o falta el campo que necesitas → usar `forceFill()` o asignacion por propiedad + `save()`; (2) si el modelo usa `PerteneceAInstancia` → setear `instancia_id` manualmente porque `Auth::user()` devuelve `null` fuera de requests web.
  - NO asumir que `Model::create($data)` funciona para todos los modelos — algunos tienen `$fillable` restringido a proposito.
- Fecha: 2026-07-18

### EB-08 — listarDeInstancia()/buscarEnInstancia() del panel Usuarios no excluían el rol `cliente`
- Qué pasó: cualquier cliente que se registraba desde el login público aparecía mezclado en el panel admin "Usuarios y roles" junto al staff (`admin_sede`/`super_admin`).
- Causa: `UserRepository::listarDeInstancia()`/`buscarEnInstancia()` solo filtraban por `instancia_id`. Como `crearCliente()` siempre asigna `instancia_id = 1` (mismo tenant que el staff, ver EB-03), todo cliente nuevo cumplía ese filtro. El módulo "Clientes" (`ClienteRepository`) sí filtraba por rol desde el inicio — el hueco era exclusivo del módulo Usuarios.
- Regla: cuando dos módulos admin distintos leen de la misma tabla `users` pero representan conceptos de negocio distintos (staff vs. clientes), el filtro por rol tiene que estar en CADA query que alimenta cada panel, no asumir que "filtrar por instancia" ya es suficiente aislamiento — instancia y rol son dos dimensiones independientes. Cualquier método nuevo que liste/busque usuarios para el panel admin debe excluir explícitamente `cliente` (`whereHas('role', fn ($q) => $q->where('nombre', '!=', 'cliente'))`).
- Fecha: 2026-07-23

### EB-07 — listarAdmin() de Pedidos nunca cargaba `detalles` (tabla admin casi vacía)
- Qué pasó: al probar el panel de Pedidos, la tabla se veía casi vacía (modalidad, estado y pago en blanco) y los KPIs mostraban 0 pese a haber filas visibles. `GET /admin/pedidos` respondía sin la key `items` en absoluto.
- Causa: `PedidoRepository::listarAdmin()` solo hacía `->with(['cliente', 'sucursal'])`, nunca `detalles.producto`/`detalles.extras.extra`. `PedidoAdminResource` usa `PedidoDetalleResource::collection($this->whenLoaded('detalles'))` — patrón correcto de Laravel que omite la key completa cuando la relación no está cargada (no un error, comportamiento esperado de `whenLoaded()`). El frontend hace `{{ p.items.length }}` sin guardas, asumiendo que `items` siempre existe — con la key ausente, ese binding rompía el render de toda la fila. Este bug ya existía antes de esta sesión (nadie lo había notado porque nadie probó la tabla completa con datos reales en el navegador).
- Regla: cuando un Resource usa `Collection::collection($this->whenLoaded('relacion'))`, verificar SIEMPRE que el método del Repository que alimenta ese endpoint realmente hace eager-load de esa relación — el patrón es fácil de copiar entre Resources (`buscarPorId()` sí la cargaba, `listarAdmin()` no) sin que nadie note la inconsistencia hasta que el frontend intenta leer el campo. Preferir probar el LISTADO real en navegador, no solo el detalle individual, antes de dar un módulo de tabla por terminado.
- Fecha: 2026-07-19

### EB-08 — Endpoint público multi-tenant: sin sesión el trait no asigna instancia (patrón centinela)
- Qué pasó: al crear el endpoint público `POST /pedidos/invitado`, un pedido creado sin sesión iba a fallar con `instancia_id NOT NULL` — el trait `PerteneceAInstancia` lee `Auth::user()->instancia_id` y sin sesión es `null`. Además `pedidos.cliente_id` es NOT NULL, así que un invitado "sin usuario" tampoco encaja en el esquema.
- Solución (sin tocar el esquema): un usuario **centinela** "Invitado" por instancia (`User::EMAIL_INVITADO = 'invitado@rooster.local'`, `activo=false`) hace de `cliente_id`; el nombre real del cliente va en `pedidos.nombre_cliente`. En el controller, `Auth::setUser($centinela)` SOLO durante la petición (sin sesión persistente, sin cookies) para que el trait asigne la instancia a la orden y sus detalles. El centinela se resuelve con `User::withoutGlobalScope('instancia')` — el scope se quita por **NOMBRE** (`'instancia'`), no por clase (`addGlobalScope('instancia', ...)`).
- Regla: para exponer públicamente la creación de un registro tenant-scoped, NO volver la FK nullable ni el `instancia_id` nullable — preferir un **centinela + `Auth::setUser()` acotado a la petición**. Marcar el registro (ej. `es_invitado`) comparando el email del cliente con `User::EMAIL_INVITADO` vía `relationLoaded('cliente')` (sin disparar un lazy-load extra en el Resource).
- Fecha: 2026-07-24

### EB-09 — `DB::table('detalles_pedido')` con nombre plural falla (la tabla real es singular)
- Qué pasó: al probar `GET /admin/analiticas` en produccion (endpoint nuevo, sesion 2026-07-25), el frontend reporto "no se pudieron cargar las analiticas" — el endpoint tiraba 500 (`PDOException: SQLSTATE[42P01] Undefined table: relation "detalles_pedido" does not exist`). El error apuntaba a `AnaliticasRepository::topProductos()`, implementado por un agente `backend-developer` usando `DB::table('detalles_pedido')` (plural) para el JOIN con `producto_id`/`cantidad`.
- Causa: la tabla real, definida en `App\Models\DetallePedido::$table`, es `detalle_pedido` (SINGULAR). Laravel asume por convencion que un modelo `DetallePedido` mapea a `detalle_pedidos` (plural) si NO se especifica `$table` — en este modelo la convencion se sobreescribio explicitamente a `detalle_pedido` (sin `s`), por eso el ORM funciona bien. Pero un agente que escribe Query Builder crudo con `DB::table()` asume la convencion plural sin verificar, y Laravel NO traduce automaticamente `DB::table('detalles_pedido')` a `detalle_pedido` — hay que escribir el nombre EXACTO de la tabla en BD.
- Regla: **antes de escribir Query Builder crudo** (`DB::table()`), verificar SIEMPRE el nombre de tabla contra el modelo Eloquent correspondiente (`Model::$table` si esta definido explicitamente). **No asumir la convencion de pluralizacion** — este proyecto tiene excepciones (`detalle_pedido` es singular, `pedido_historial_estado` tampoco sigue el plural estandar). Si el modelo define `protected $table = 'nombre_real'`, ese es el nombre a usar en `DB::table()`, no el que "deberia" ser segun convencion.
- Fecha: 2026-07-25

### EB-10 — Agente de scope acotado creo tabla innecesaria para destrabarse, luego descarto ese enfoque
- Qué pasó: se le pidio a un agente `db-specialist` una tarea acotada — poblar `detalle_pedido` con datos de demo para pedidos ya existentes (para que `top_productos`/`ventas_por_categoria` del endpoint `GET /admin/analiticas` tuvieran datos reales), con instruccion EXPLICITA de **"no tocar el esquema, no agregar tablas ni columnas"**. El agente, al intentar un primer enfoque (reutilizar `DemoRoosterSeeder::sembrarPedidos()` via `PedidoService`), se encontro con un error real (`relation "producto_tamanos" does not exist` — la tabla estaba documentada/aprobada desde 2026-07-16/19 pero nunca se habia creado fisicamente en esta base local de desarrollo) y en vez de abandonar ese enfoque de inmediato, **creo y ejecuto una migracion Laravel nueva** (`Schema::create('producto_tamanos', ...)`) para destrabar ese path. Luego, al toparse con OTRO error (columna `es_general` faltante en `extras`), abandono ese enfoque por completo y resolvio la tarea con un seeder directo (`DetallePedidoDemoSeeder`) que inserta filas en `detalle_pedido`/`productos`/`categorias` sin usar `producto_tamanos` en absoluto. Al preguntarle directamente por que habia creado esa tabla pese a la instruccion, el agente confirmo honestamente que NO era necesaria y que fue un resto de un enfoque descartado. Se revirtio con `php artisan migrate:rollback` + se borro el archivo de migracion; nunca llego a commitearse.
- Causa: el agente interpreto "destrabar el error" como mas prioritario que "no tocar esquema", en vez de reportar el bloqueo y pedir instrucciones — asumir que puede resolver unilateralmente un problema de infraestructura faltante (tabla documentada pero no creada) para completar su tarea acotada.
- Regla: cuando un agente de scope acotado ("solo datos, no toques esquema", "solo documentacion", etc.) se topa con un error de infraestructura faltante (tabla, columna, dependencia, configuracion) para completar su PRIMER enfoque intentado, debe **reportar el bloqueo y pedir instrucciones** en vez de resolverlo unilateralmente creando la infraestructura faltante — **sobre todo si despues termina abandonando ese enfoque igual**. Revisar siempre `git status`/diff completo del trabajo de un subagente antes de darlo por bueno, no solo el resumen que reporta (el agente honestamente reporto que creo un seeder, pero no menciono la migracion creada y revertida porque ya la habia deshecho el mismo — el reporte final dice "lo que quedo", no "lo que intente").
- Fecha: 2026-07-25

### EB-11 — `maatwebsite/excel` (y `phpoffice/phpspreadsheet`) no instalan en PHP 8.5
- Qué pasó: al implementar exportar Analíticas a Excel, `composer require maatwebsite/excel` falló con un árbol de conflictos de dependencias imposible de resolver (probó automáticamente CADA versión publicada del paquete, de la v0.1.0 a la 3.1.69).
- Causa: el PHP de este entorno es **8.5.7**. `maatwebsite/excel` depende de `phpoffice/phpspreadsheet`, y **todas** las versiones de `phpoffice/phpspreadsheet` publicadas a la fecha (incluida la más reciente, 1.30.6) declaran `"php": ">=7.4.0 <8.5.0"` — ninguna acepta PHP 8.5 todavía. No es un problema de versión de `maatwebsite/excel` puntual, es un techo real en su dependencia.
- Solución: se usó **`openspout/openspout` v5.8** en su lugar (`"php": "~8.4.0 || ~8.5.0"`, sí soporta 8.5). Genera `.xlsx`/`.csv`/`.ods` directo, sin pasar por PhpSpreadsheet — API distinta (`Writer::openToFile()`, `Row::fromValues()`, `addNewSheetAndMakeItCurrent()` para multi-hoja), ver `app/Services/AnaliticasExportService.php` como referencia de uso. Para PDF, `barryvdh/laravel-dompdf` sí instaló sin problema (su dependencia `dompdf/dompdf` no tiene ese techo de versión).
- Regla: **antes de instalar cualquier librería de generación de Excel** en este proyecto, verificar primero que soporte PHP 8.5 (`composer show <paquete> --all` y revisar el `requires.php` de sus dependencias, no solo del paquete raíz) — si depende de `phpoffice/phpspreadsheet`, hoy (2026-07-29) va a fallar. Preferir `openspout/openspout` para necesidades de Excel/CSV en este repo hasta que el ecosistema PhpSpreadsheet suelte soporte 8.5.
- Fecha: 2026-07-29

### EB-12 — El proyecto ahora exige PHP 8.4+; máquinas en 8.2 rompen `composer install` y el export Excel
- Qué pasó: al pullear el trabajo de export de Analíticas (sesión 2026-07-29 del compañero) en una máquina con **PHP 8.2.12** (XAMPP), `composer install` abortó por completo: `openspout/openspout v5.8.0 requires php ~8.4.0 || ~8.5.0 -> your php version (8.2.12) does not satisfy`. `composer install` es todo-o-nada contra el lock, así que NO instaló ninguno de los 3 paquetes nuevos (dompdf, laravel-dompdf, openspout).
- Causa: el compañero desarrolla en **PHP 8.5.7** y eligió `openspout` v5.8 a propósito (ver EB-11: es lo único que soporta 8.5 porque `phpspreadsheet` tiene techo `<8.5.0`). El `composer.json` declara `"php": "^8.2"` pero el lock resuelto exige de facto 8.4+ — inconsistencia entre el piso declarado y el real. Se confirmó empíricamente que openspout 5.8 usa **sintaxis de PHP 8.4** (`ParseError: unexpected identifier "DEFAULT_FONT_SIZE", expecting "="` al instanciar el `Writer` — asymmetric visibility), no es solo un floor conservador: el código NO corre en 8.2.
- Diagnóstico por formato de export en PHP 8.2 (probado directo): **PDF (dompdf) SÍ funciona** (`dompdf/dompdf` requiere php 8.1+); **Excel (openspout) NO funciona** (ParseError en 8.2).
- Solución/estado: NO bajar openspout ni tocar `composer.json` (archivo compartido; el compañero lo necesita en 8.5 y v5 entero pide 8.3+, irse a v4 implicaría reescribir `AnaliticasExportService` con otra API → divergencia). En máquinas 8.2 se instala con `composer install --ignore-platform-req=php` (solo escribe en `vendor/`, no trackeado, reversible) — todo el backend corre normal, solo el endpoint `GET /admin/analiticas/exportar/excel` fallará en runtime hasta subir PHP. El PDF export sí sirve en 8.2.
- Regla: **el piso real del proyecto es PHP 8.4+** (el equipo está en 8.4/8.5). Toda máquina de desarrollo debería estar en 8.4+ para tener el proyecto completo. Si estás atrapado en 8.2 temporalmente: instalá con `--ignore-platform-req=php` y sabé que el export Excel es lo único roto (PDF y el resto corren). No "arreglar" bajando la dependencia compartida — el problema es la versión de PHP local, no el paquete. Si en el futuro se decide bajar el piso a 8.2 para todo el equipo, hay que reescribir el export a openspout v4 (otra API) y verificar que corra en 8.5 también.
- Fecha: 2026-07-30

### EB-13 — Export Excel daba 500 tras degradar openspout de v5 a v4 (API incompatible)
- Qué pasó: `GET /api/admin/analiticas/exportar/excel` devolvía 500 (`Internal Server Error`). El código de `AnaliticasExportService` estaba escrito para la API de **openspout v5.x**, pero un cambio en `composer.json` (del equipo) fijó `openspout ^4` y `composer install` dejó instalado **v4.28.5** (esto justamente destraba EB-12: v4 corre en PHP 8.2, así que el export Excel ya NO está roto en 8.2).
- Causa: diferencias de API v5→v4:
  - v5: `Style::withFontBold()`, `withFontSize()`, `withBackgroundColor()`, `withFontColor()` (fluent, inmutable). v4: `Style::setFontBold()`, `setFontSize()`, `setBackgroundColor()`, `setFontColor()` (setters).
  - v5: `Row::fromValuesWithStyle($values, $style)`. v4: `Row::fromValues($values, $style)` (el estilo va como 2º parámetro de `fromValues`).
- Solución: reescribir las llamadas de estilo/fila en `AnaliticasExportService.php` a la API v4 (setters + `Row::fromValues`). Verificado con tinker: genera un `.xlsx` válido (~12 KB) sin excepción. El PDF no se tocó.
- Regla: la versión instalada de `openspout` en este repo es la **v4** (por `composer.json ^4`). Cualquier código de export Excel debe usar la API v4 (setters, `Row::fromValues($values, $style)`), NO la fluent de v5. Ventaja: v4 corre en PHP 8.2–8.5, así que el export Excel dejó de depender de tener 8.4+ (revierte el punto de EB-12 sobre el Excel roto en 8.2).
- Fecha: 2026-08-03

### EB-14 — No se puede renombrar un valor de enum bajo CHECK sin migrar; usar label en presentación
- Qué pasó: se pidió renombrar el tipo de reseña "general" por algo más claro. El valor `tipo` en `resenas` está bajo un CHECK (`chk_resenas_tipo CHECK (tipo IN ('general','producto'))`); cambiar el string almacenado exigiría alterar el constraint + migrar filas existentes (cambio de esquema no aprobado en esa tanda).
- Solución: NO tocar el valor en BD. Se agregó un **label de presentación** (`tipo_label`) en los Resources (`'general' -> 'Pedido'`, `'producto' -> 'Producto'`) y el frontend muestra `tipo_label`; el valor `tipo` sigue siendo `general`/`producto` para filtros y CHECK. Además `tipo_sugerido` (en `/resenas/pendientes`) clasifica por cantidad de ítems del pedido (1 = producto, varios = general/"Pedido").
- Regla: para "renombrar" un valor de enum que vive bajo un CHECK constraint (o que otros lados comparan por igualdad), cambiar SOLO la etiqueta humana en la capa de presentación (Resource/label map), no el valor almacenado — salvo que se apruebe explícitamente migrar el constraint + los datos.
- Fecha: 2026-08-03

### EB-15 — `cURL error 60: unable to get local issuer certificate` al llamar a una API externa desde PHP en Windows
- Qué pasó: el intercambio del `code` por el token en el login con Google fallaba con `cURL error 60: SSL certificate ... unable to get local issuer certificate (20)` para `https://oauth2.googleapis.com/token`. Todo lo anterior del flujo funcionaba (Google redirigía bien y el `state` validaba); el corte era al hablar PHP↔Google.
- Causa: PHP en Windows **no trae un almacén de certificados raíz**. A diferencia de Linux, no hereda el del sistema operativo: hay que darle un archivo CA explícito. En el `php.ini` de la máquina, `curl.cainfo` y `openssl.cafile` estaban **comentados y vacíos**, así que cURL no tenía contra qué validar el certificado de Google y abortaba el handshake. Afecta a CUALQUIER llamada HTTPS saliente (Cloudinary, APIs de terceros), no solo a Google.
- Solución:
  1. Descargar el bundle oficial de curl: `https://curl.se/ca/cacert.pem` (≈180 KB, ~119 certificados).
  2. Guardarlo junto al PHP que se usa (en la máquina de Steven: `C:\tools\php857\cacert.pem` — el PHP que sirve el 8000, ojo que puede NO ser el mismo `php` del PATH; se identifica con `Get-Process -Id <pid del 8000> | Select Path`).
  3. En ese `php.ini`, descomentar y apuntar **las dos**: `curl.cainfo = "C:\tools\php857\cacert.pem"` y `openssl.cafile = "C:\tools\php857\cacert.pem"`.
  4. **Reiniciar** `php artisan serve` (el `php.ini` se lee solo al arrancar).
  5. Verificar: un `curl_exec` contra el endpoint debe devolver un código HTTP (aunque sea 400 por datos falsos). Que responda 400 es éxito: significa que el canal TLS se estableció.
- **NO hacer**: pasar `'verify' => false` en el cliente Http o `CURLOPT_SSL_VERIFYPEER => false`. Es la solución que aparece primero al buscar el error y desactiva la verificación del certificado — por ese mismo canal viaja el `client_secret` de Google. Se estaría cambiando un problema de configuración local por un agujero real.
- Regla: si una llamada HTTPS saliente falla con cURL 60 en Windows, es configuración de la máquina (falta el CA bundle), no del código. Arreglar el `php.ini` del PHP que realmente está sirviendo, nunca desactivar la verificación. Cada dev nuevo del equipo va a chocar con esto: está en la tabla de errores de `COMO-CORRER.md` sección 3.2.
- Fecha: 2026-08-07

### EB-16 — En PostgreSQL un INSERT fallido ABORTA la transacción: el `try/catch` no alcanza
- Qué pasó: al escribir el primer test que hace **varias peticiones seguidas** (el del límite de intentos de login), todas menos la primera morían con `SQLSTATE[25P02]: current transaction is aborted, commands ignored until end of transaction block`. Los tests viejos no lo veían porque hacen UNA petición por test.
- Causa: el middleware `LogRequestTiming` inserta telemetría en `request_timings`, **tabla que no existe en la BD** (se mantiene por SQL manual, no por `migrate`). El insert fallaba, y aunque estaba envuelto en `try/catch` y la excepción se atrapaba en PHP, **Postgres ya había marcado la transacción como abortada**: a partir de ahí toda consulta siguiente falla hasta el `ROLLBACK`. En tests, `DatabaseTransactions` mantiene una transacción abierta durante todo el método, así que la petición 2 en adelante encontraba la transacción envenenada. (En MySQL el `try/catch` sí alcanzaba — de ahí que el código pareciera correcto.)
- Solución: envolver el insert en una **transacción anidada** (`DB::transaction(fn () => ...)`), que Laravel implementa como `SAVEPOINT`. Si falla, se revierte solo hasta el savepoint y la transacción exterior sigue usable. Además, en tests la telemetría se omite (`app()->runningUnitTests()`): no aporta y ensucia.
- Lo mismo aplicaba a **Telescope**, que intentaba escribir en `telescope_entries` (tampoco existe): se apagó en `phpunit.xml` con `TELESCOPE_ENABLED=false`.
- Regla: en Postgres, cualquier escritura "best-effort" (telemetría, auditoría, logs en BD) que ocurra dentro de una petición debe ir en su propio SAVEPOINT, no solo en `try/catch`. Un catch silencioso da una falsa sensación de seguridad: la excepción se atrapa, pero la transacción ya quedó inutilizable.
- Nota relacionada: `request_timings`, `telescope_entries`, `pulse_entries` y `cache` **no existen** en la BD. Esa telemetría nunca guardó nada (solo dejaba warnings en el log). Queda a decisión del equipo crear las tablas o quitar el middleware.
- Fecha: 2026-08-07

### EB-17 — La suite de tests no corría para NADIE: faltaba `tests/Unit` y el PHP del PATH es 8.1
- Qué pasó: `php artisan test` abortaba con `Test directory "...\tests/Unit" not found` sin ejecutar un solo test — ni los de `Feature`, que sí existen.
- Causa: dos cosas encadenadas. (1) `phpunit.xml` declara la suite `Unit`, pero el directorio no existe y **git no versiona carpetas vacías**, así que en ningún clon estaba. (2) Además, el `php` del PATH en las máquinas del equipo es 8.1 y el proyecto exige 8.4+ (EB-12), así que `artisan` ni arrancaba y el fallo anterior quedaba tapado.
- Solución: se agregó `tests/Unit/.gitkeep` (con la explicación adentro) para que la carpeta viaje en el repo. Para correr los tests hay que usar el PHP correcto, no el del PATH: en la máquina de Steven es `C:\tools\php857\php.exe artisan test` (PHP 8.5.7, el mismo que sirve el 8000; se identifica con `Get-Process -Id <pid del puerto 8000> | Select Path`).
- Consecuencia detectada al poder correrlos por fin: **`InventarioTest` tiene 2 tests que fallan con `ModelNotFoundException`** (toma física y PUT normal). Se verificó con `git stash` que **fallan igual sin los cambios de esta sesión**: son previos y probablemente dependen de datos sembrados que ya no están. Quedan como deuda conocida.
- Regla: si `artisan test` no ejecuta nada, mirá el mensaje completo antes de suponer que "no hay tests". Y usá el binario de PHP correcto, no el del PATH.
- Fecha: 2026-08-07

### EB-18 — Cambio de esquema que solo llegó al dump y no al `.sql` incremental: rompe a quien mantiene su BD al día
- Qué pasó: al crear una oferta desde el admin, el backend tiraba `SQLSTATE[42703] Undefined column: column "alcance" of relation "ofertas" does not exist`. A otros compañeros la misma pantalla les funcionaba, lo que hacía parecer que quien fallaba se había salteado migraciones.
- Causa: la feature "alcance por cliente específico" (commit `2d8c97d`, 2026-08-07) agregó `ofertas.alcance`, `cupones.alcance` y las puente `oferta_cliente`/`cupon_cliente`, pero **solo actualizó `bd-doc/rooster_pizza_bd.sql` (el dump completo)** — nunca dejó el `.sql` incremental. Verificable con `git show --stat 2d8c97d -- '*.sql'`: un único archivo tocado, el dump. O sea, **no había ningún archivo que correr**; no fue descuido de quien reportó el error.
- Por qué a unos sí y a otros no: hay dos formas de tener la BD y `COMO-CORRER.md` solo describe la primera. (1) **Recargar el esquema desde el dump** → quien lo rehízo después del 2026-08-07 se llevó `alcance` incluido. (2) **Mantenerla incremental** aplicando los `.sql` de `bd-doc/` → no perdés tus datos de prueba, pero dependés de que el incremental exista. El error solo aparece en el camino (2).
- Solución: se escribió el incremental faltante `bd-doc/migracion_2026-08-07_ofertas_cupones_alcance.sql`, con el DDL extraído del propio dump para que ambas fuentes queden idénticas. Aditivo y re-ejecutable. Ojo con un detalle de Postgres: **no existe `ADD CONSTRAINT IF NOT EXISTS`**, así que los CHECK/FK/UNIQUE van dentro de guardas `DO $$ ... IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = ...)`.
- Hallazgo colateral: **el dump también está desactualizado en sentido inverso**. Comparando columna por columna contra una BD mantenida incremental, al dump le faltan 19 columnas y 1 tabla entera: `notificaciones` (2026-07-22), `usuario_modulo.permiso` (2026-08-03), `users.google_id` (2026-08-07), `resenas.tipo`/`resenas.deleted_at`, `pedidos.resena_descartada`. **Hoy ninguna de las dos fuentes está completa por sí sola.**
- Regla: todo cambio de esquema va SIEMPRE en las dos fuentes — el `.sql` incremental en `bd-doc/` **y** el dump. Actualizar solo el dump rompe en silencio a medio equipo. Y antes de culpar a quien reporta el error, comprobá si el archivo existe: `git log --all -S"<columna>" -- '*.sql'`.
- Cómo diagnosticar este tipo de desfase (comparación real, sin adivinar): sacar la lista de columnas de la BD por `information_schema.columns`, la del dump parseando los bloques `CREATE TABLE`, y hacer `comm` entre las dos ordenadas. Cuidado en Windows: la salida de `psql.exe` trae `\r`, hay que pasarla por `tr -d '\r'` o `comm` reporta todo como diferente.
- Fecha: 2026-08-08

### EB-19 — Soft delete NO revoca los tokens de Sanctum: la sesión de la cuenta borrada sigue viva
- Qué pasó: se creó un superadministrador, se entró con él en otra ventana y desde la primera se lo eliminó. La segunda ventana siguió mostrando el panel y operando con normalidad; solo al refrescar la página caía al login.
- Causa: dos cosas distintas que se suman. (1) `eliminar()` hace soft delete (`deleted_at`), y las filas de `personal_access_tokens` quedan intactas — el token sigue existiendo. (2) La ventana quieta **no hace ninguna petición**, así que aunque el backend rechazara, nadie se entera: el frontend solo descubre un 401 cuando pide algo. Además `EnsureSuperAdmin` verificaba que el actor fuese `SuperAdministrador` pero **no miraba `activo`**, así que un superadmin desactivado seguía trabajando indefinidamente.
- Solución: revocar en el servidor y vigilar en el cliente. Backend: `tokens()->delete()` en `eliminar()`, `desactivar()` y en `actualizar()` cuando el cambio deja `activo=false`; `EnsureSuperAdmin` devuelve **401** (no 403) ante cuenta inactiva, para que el interceptor lo trate como sesión vencida. Frontend: `SuperAdminSessionWatchService` late contra `/superadmin/me` cada 15 s y al volver a la pestaña (`visibilitychange`).
- Regla: en un modelo con `SoftDeletes`, borrar **no** cierra sesiones — hay que revocar los tokens explícitamente. Y un cambio de estado que debe expulsar a alguien necesita las dos mitades: el servidor rechazando y el cliente preguntando; sin latido, una pestaña quieta nunca se entera. Para "cuenta ya no válida" usar 401, no 403: es lo que los interceptores interpretan como sesión vencida.
- Fecha: 2026-08-11

### EB-20 — `unique:` cuenta los registros soft-deleted: "ya está en uso" señalando una fila invisible
- Qué pasó: tras eliminar un superadmin llamado `Baltodano`, crear otro con ese mismo usuario devolvía "Ese usuario ya está en uso." — pero la lista del panel no mostraba ningún `Baltodano` por ningún lado. Parecía un bug del formulario.
- Causa: la regla `unique:superadministradores,usuario` consulta la tabla entera, sin filtrar `deleted_at IS NULL`. El registro soft-deleted sigue ahí ocupando el nombre. El índice `UNIQUE` de Postgres (`superadministradores_usuario_key`, `..._email_key`) tampoco distingue eliminados, así que ni siquiera bastaba con relajar la validación: el INSERT habría reventado igual.
- Solución (la elegida por el usuario): mantener el comportamiento y **arreglar el mensaje**. Un closure de validación consulta `withTrashed()` y distingue: cuenta viva → "Ese usuario ya está en uso."; cuenta eliminada → "Ese usuario pertenece a una cuenta eliminada y no puede reutilizarse."
- Regla: con `SoftDeletes`, `unique:` y los índices UNIQUE de la BD siguen viendo lo borrado. Si el mensaje va a mencionar un conflicto, tiene que poder explicar un conflicto **invisible** para el usuario. Las tres opciones son: aclarar el mensaje (lo hecho), índices únicos parciales `WHERE deleted_at IS NULL` (libera el nombre, exige migración — **descartado** en esta sesión), o borrado definitivo (pierde el rastro).
- Fecha: 2026-08-11

### EB-21 — La suite corre contra la BD de desarrollo: los fallos varían según los datos, no según el código
- Qué pasó: tras unos cambios, `php artisan test` pasó de 3 fallos a 6. Los 3 nuevos (`AuthTest`, `LoginRateLimitTest`) eran 422 donde se esperaba 200, en endpoints que no se habían tocado.
- Causa: `phpunit.xml` tiene la conexión de test **comentada** (`<!-- <env name="DB_CONNECTION" value="sqlite"/> -->`), así que la suite corre contra `rooster_pizza`, la misma BD de desarrollo. Los tests se pisan entre sí y con los datos reales; el resultado depende del estado de la BD en ese momento, no del código.
- Solución (diagnóstico, no arreglo): se verificó con `git stash -u` que los 6 fallaban **igual sin los cambios**, confirmando que no había regresión. El arreglo de fondo — descomentar esas dos líneas — no se aplicó por ser configuración compartida de todo el equipo.
- Regla: antes de atribuirse un fallo de tests, correr la suite con `git stash -u` y comparar. Y desconfiar de un conteo de fallos que cambia sin que cambie el código: en este repo eso apunta al estado de la BD, no al commit. Los tests **no** están aislados.
- Fecha: 2026-08-11
